import { StyleSheet } from "react-native";

export const styles = StyleSheet.create ({
  textinput: {
    display: "none",
  },
  input: {
    fontSize: 20,
  },
  component: {
    alignItems: "center",
    marginTop: 10,
  },
  pressable: {
    alignItems: "center",
    borderWidth: 1,
    borderColor: "#1D57F6",
    borderRadius: 5,
    width: "70%",
  }
});